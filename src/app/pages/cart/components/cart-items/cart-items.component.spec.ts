import { CartItemsComponent } from "./cart-items.component";


describe('CartItemsComponent', () => {
  let fixture: CartItemsComponent;

  beforeEach(() => {
  it('should create cart items component', () => {
    expect(fixture).toBeTruthy();
  })
});